import {JSX} from "react";
import {stickingProps} from "@/modals/types";
import {Card} from "react-native-paper";


export default function StickingVisualizer({pattern, tempo, isPlaying}:stickingProps):JSX.Element{
    console.log("StickingVisualizer()");

    return (
        <Card >

        </Card>
    )

}

